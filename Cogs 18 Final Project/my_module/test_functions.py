from functions import *

# Tests that the easy difficulty function is callable
def test_1():
    assert callable(easy)
    
# Tests that the time_up_hard function is callable
def test_2():
    assert callable(time_up_hard)